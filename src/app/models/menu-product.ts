export interface MenuProduct {
    productId?: number,
    productImage?: string,
    productCategory?: string,
    desciption?: string,
    ingredients?: string,
    calories?: number,
    quality?: number,
    price?: number,
}